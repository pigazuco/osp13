---
    - name: PREPARE DOCKER
      hosts: osp-director
      remote_user: root
      gather_facts: no
      tasks:
        - name: Copy deploy_project.yml
          copy:
            src: files/daemon.json
            dest: /root/daemon.json